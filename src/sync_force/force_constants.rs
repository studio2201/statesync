use std::time::Duration;

pub const FORCE_PAGE_TIMEOUT: Duration = Duration::from_secs(60);
pub const FORCE_UPDATE_TIMEOUT: Duration = Duration::from_secs(30);
pub const FORCE_ITEM_CAP: usize = 100_000;
