# Zero-configuration compose: image + port + config volume only.
services:
  statesync:
    image: ghcr.io/studio2201/statesync:latest
    container_name: statesync
    restart: unless-stopped
    ports:
      - "4601:4601"
    volumes:
      - ./config:/config
